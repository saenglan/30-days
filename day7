//Use forEach to iterate through an array

function main() {
    const n = parseInt(readLine(), 10);

    const arr = readLine().split(' ').map(arrTemp => parseInt(arrTemp, 10));
    
    let arr2 =[];
    
    let string = '';
    
    for(let i = arr.length-1; i>=0; i--){
            arr2.push(arr[i]+ ' ');
    }
    
    arr2.forEach(item => string += item);
    console.log(string);
}
